import { FC } from "react";
import Card from "@mui/material/Card";

const Music: FC = () => {
    return (
        <Card sx={{ minHeight: 796 }}>
            <div>Music</div>
        </Card>
    );
};

export default Music;
